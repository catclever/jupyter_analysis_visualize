# Gemini Agent Rules

## Testing
- **Auto-cleanup**: Any temporary test scripts created for verification or reproduction (e.g., `test_*.py`, `reproduce_*.py`) MUST be deleted IMMEDIATELY after the verification step is complete. Do not leave them for the user to clean up.
